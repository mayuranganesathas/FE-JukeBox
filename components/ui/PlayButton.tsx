export interface PlayButtonProps {}

export const PlayButton = ({}: PlayButtonProps) => {
  return (
    <div className="w-1/2 h-1/2 border-8">
      <div>Song Title</div>
    </div>
  );
};

export default PlayButton;
